2026.6.29
在原来按比例特征融合的基础上进行了代码的修改，使用gated—fusion模块。
train_coco 是 gated 训练的（否则加 FUSION_TYPE=linear）

关键点：fusion_type 必须与训练一致（最易错）
USE_ILR=1 时脚本默认传 --fusion_type gated。所以：
若 train_coco 是用 gated 训练的 → 当前命令✓正确
若 train_coco 是旧的 linear checkpoint → 必须加 FUSION_TYPE=linear：
FUSION_TYPE=linear USE_ILR=1 MEMORY_ID=coco bash scripts/eval_flickr30k_meacap.sh train_coco 0 14

***跑「外 + 门控融合」实验，核心前提是：评估必须用 gated 训练出来的 checkpoint（门控带参数，不能拿旧 linear 权重直接评估）。完整流程如下。
前提检查
ILR 邻居 JSON 已存在（没有则先跑一次）
bash scripts/build_ilr_neighbors_coco.sh 0
GPU 可用（若有MIG，则MIG 环境需先绑定）
export CUDA_VISIBLE_DEVICES=MIG-a414fbb5-4bcd-543c-a1a5-05c11a248313
python -c "import torch; print(torch.cuda.device_count())"   # 必须 >=1
第 1 步：用 gated 训练（必做）
脚本默认就是 gated，直接训练：
bash scripts/train_coco.sh 0
检查日志出现 'fusion_type': 'gated'
权重存到 checkpoints/train_coco/coco_prefix-0014.pt（COCO 训 15 epoch，0–14）
第 2 步：用 gated 评估（运行 eval_coco_meacap.sh）
USE_ILR=1 MEMORY_ID=coco bash scripts/eval_coco_meacap.sh train_coco 0 14
参数含义：
train_coco：checkpoint 目录名（实验名）
0：GPU 编号
14：用第 14 个 epoch 的权重
⚠️ 必须加 USE_ILR=1——只有它为 1，脚本才会把 --use_ilr --fusion_type gated 传给 validation.py。否则不会启用门控融合，gated checkpoint 里的 fusion.* 权重会被忽略，结果错误。
验证是否生效
评估日志的 args 应包含：
'use_ilr': True, 'fusion_type': 'gated'
模式行应为：
Validation mode: MeaCap InvLM (memory retrieve-then-filter) + ILR feature fusion
做 linear 对照（消融对比）
如果要和旧的线性加权对比，需另训一个 linear 版本再评估：
# 训练 linear 版（建议用不同实验名，避免覆盖 gated 权重）
FUSION_TYPE=linear bash scripts/train_coco.sh 0   # 注意:会写到 checkpoints/train_coco
# 评估 linear
FUSION_TYPE=linear USE_ILR=1 MEMORY_ID=coco bash scripts/eval_coco_meacap.sh train_coco 0 14
