2026.8.1
当前crosstn分支是在update_3.0基础上做的改进，即在门控交叉注意力机制上（gated_crossattn fusion）做的改进.
实验效果不太好

当前链路
完整链路：
e_primary 512-d
CrossAttnFusion（外融合）
e_retrieved K×512
e_fused 512-d
MappingNetwork（Projector）
Soft Prompt
GPT-2
调用位置（推理）：
primary = model.fusion(primary, rt_features)   # 先融合
return model.mapping_network(primary)          # 再投影
